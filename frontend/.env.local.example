API_INTERNAL_URL=http://127.0.0.1:8000
# Browser chat in production: set to the Railway HTTPS origin (no trailing slash).
# Leave empty locally so the Next.js rewrite to 127.0.0.1:8000 is used.
NEXT_PUBLIC_API_URL=

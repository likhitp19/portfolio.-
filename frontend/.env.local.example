API_INTERNAL_URL=http://127.0.0.1:8000
# Production (Vercel): set both to the Railway HTTPS origin, no trailing slash.
# Leave NEXT_PUBLIC_API_URL empty locally so the Next.js rewrite to :8000 is used.
NEXT_PUBLIC_API_URL=
